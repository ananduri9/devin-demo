{
  "name": "devin-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "start": ". ./.env && node dist/server.js",
    "dev": ". ./.env && nodemon --watch src --ext ts --exec 'ts-node src/server.ts'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "nodemon": "^3.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
